-each post, i in posts
  -if (i % 3 == 0)
    |<div class="row">
  .span4
    h2
      a(href='/u/#{post.user}') #{post.user}说
    p
      small #{post.time}
    p #{post.post}
  -if (i %3 == 2)
    |</div>
  -if (posts.length % 3 != 0)
    |</div>
